# Skill disponibili

- **meteo** — Meteo attuale + min/max di oggi per una città (Open-Meteo, nessuna key). (es. 'meteo a Roma')

Per pianificare una skill nel tempo: comando /task o keyword (programma, programmami, pianifica, ricordami).
