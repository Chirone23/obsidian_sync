# Serverino Bot — MVP v2 (§8 opzione B)
# Single asyncio loop, no extra concurrency libs.

python-telegram-bot[job-queue]==21.6   # core + JobQueue nativa per lo scheduler
openai==1.54.4                          # client OpenAI-compatible → api.deepseek.com
httpx==0.27.2                           # chiamate dirette (meteo Open-Meteo, /user/balance)
python-dotenv==1.0.1                    # carica .env

# NOTE deliberate (§8):
# - niente croniter: cron solo "ogni giorno HH:MM" / "ogni N ore", parsing custom
# - niente SDK deepseek (non esiste): si usa openai puntato a api.deepseek.com
# - niente MCP/obsidian client: contesto letto da filesystem
